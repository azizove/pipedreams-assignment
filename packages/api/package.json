{
  "name": "api",
  "version": "1.0.0",
  "description": "This project was bootstrapped with Fastify-CLI.",
  "main": "app.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "tap \"test/**/*.test.js\"",
    "start": "npm run seed && fastify start -l info app.js",
    "dev": "npm run seed && fastify start -w -l info -P app.js",
    "seed": "node seeds/seed.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/autoload": "^5.0.0",
    "@fastify/mongodb": "^7.0.0",
    "@fastify/sensible": "^5.0.0",
    "dotenv": "^16.3.1",
    "fastify": "^4.20.0",
    "fastify-cli": "^5.7.1",
    "fastify-plugin": "^4.0.0",
    "mongo-seeding": "^3.7.2"
  },
  "devDependencies": {
    "tap": "^16.1.0"
  }
}
